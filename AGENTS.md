# Lineage 开发指令

## 项目概述

使用 React + TypeScript + Vite + Tailwind + Framer Motion + Recharts 开发的苹果官网风格可视化页：陈列 2007 年至今正式发售的 iPhone 与 iPad，中英文切换，里程碑卡片，首发价折线图（Hover 出图+价），芯片性能图，全规格对比表。

## 开发规范

- 函数式组件 + Hooks
- Tailwind 写样式；图表用 Recharts 自定义 Tooltip
- 设备与芯片数据全部放 `src/data`，UI 不写死机型列表
- 文案走 i18n 字典或 `Localized` 字段
- TypeScript 严格；Device 类型统一
- 组件可复用：卡片、Tooltip、筛选

## 设计要求

- **浅色苹果风**，不要做成默认黑底仪表盘
- 背景 `#f5f5f7`，文字 `#1d1d1f`，强调 `#0071e3`
- 系统字体栈，大标题紧、字重 600–700，副文 `#6e6e73`
- 动效短、位移小；可加少量深色满幅里程碑区块
- 移动端适配：导航折叠、表与图横滑

## 数据与合规

- 本站是粉丝向 / 教育向产品史，页脚加一行：非 Apple 官方网站，商标归其权利人
- **禁止**整段复制 Apple.com 营销文案；总结用自己的短句
- 规格、价格、日期用公开信息；不确定就留 `null` 并在表里显示「—」，不要编造跑分
- 芯片图必须有口径说明（相对指数或第三方跑分量级）
- 收录要全：不允许只做 6 个 iPhone 交差；按 TECH_DESIGN 清单逐代补数据
- 实现时请核对「构建当下」已发布的最新一代，补进 data

## 图表注意事项

- 价格 Tooltip 必须绑定当前 `payload` 的 deviceId，展示对应图与价格，禁止永远显示同一张图
- 同一年多机型：用 family 分线或 X 轴微偏移，避免点完全重叠无法 hover
- 移动端 hover 不稳时改为 click 选中
- 图表左右留白，不要贴齐屏幕裁掉最后一个点

## 性能

- 图片统一小尺寸（卡片 < 400px 宽），懒加载
- 网格不要一次挂载超大无压缩 PNG
- `prefers-reduced-motion` 时关闭 whileInView 位移
- Recharts 数据来自 memo，避免语言切换外的无谓重绘

## 验收清单

- [ ] iPhone / iPad 从初代到最新正式代均有条目（允许个别冷门 SKU 合并说明）
- [ ] 中/EN 切换覆盖导航、Hero、图例、表头、详情、页脚
- [ ] 语言写入 localStorage
- [ ] 设备卡片可打开详情
- [ ] 里程碑区块有重点机型与滚动微动效
- [ ] 价格折线 Hover（或 tap）显示该机型图 + 名称 + 起售价，对齐数据点
- [ ] 芯片图有趋势与数据口径
- [ ] 规格表含：年、机型、芯片、内存、屏幕、分辨率、相机、电池
- [ ] 表可筛产品线、可搜、可多选高亮
- [ ] 浅色苹果风，非官方声明在页脚
- [ ] 移动端可完整走查
